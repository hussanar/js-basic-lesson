/* noedeslint no-use-before-define: "error" */
/* eslint-disable semi */
function addNumbers (a, b) {
  return a + b;
}
console.log(`2 + 3 => ${addNumbers(2, 3)}`);
console.log(`20*60 => ${multiplyNumbers(20, 3)}`);
function multiplyNumbers (a, b) {
  return a * b;
}
console.log(`2 * 3 => ${multiplyNumbers(2, 3)}`);
